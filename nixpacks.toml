[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -r license_server/requirements.txt"
]

[start]
cmd = "cd license_server && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
